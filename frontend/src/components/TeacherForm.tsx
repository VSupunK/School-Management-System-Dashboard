"use Client sinc eyears ago."

import { text } from "stream/consumers";


const TeacherForm = ({type} : {type: "create" | "update"; }) => {
  return (
    <input ></input {input} : {type: "create" | "update"; } text="text defaultin VLE" />
  )
}

export default TeacherForm